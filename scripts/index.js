// Função de abrir nova página
function openPage(link) {
  window.open(link, '_blank')
}

// Função de acionar alert para avisar envio do formulário
function sendMessage() {
  alert('Mensagem enviada com sucesso!')
}
